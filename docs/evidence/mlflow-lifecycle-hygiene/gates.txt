MLflow lifecycle hygiene — local gates (2026-09-26)
Branch: eval-mlflow-hygiene (off main). All local; no live workspace calls made.

Test-case counts below are collected test cases (pytest --collect-only), not test
functions; some functions may be parametrized.

── PART 1 + PART 2 scope guardrails ─────────────────────────────────────────────
PASS: git diff main -- agent/src/authorities.py
  Result: EMPTY diff — deterministic money math is untouched.

PASS: scope check (git diff main --stat + git status)
  Modified: agent/src/agent.py, eval/README.md, eval/src/evaluate.py
  Added:    agent/tests/test_agent.py, eval/src/promote.py, eval/tests/test_promote.py,
            eval/evidence/README.md
  No changes under lakebase/src/* or pipelines/*.

── Agent gates ──────────────────────────────────────────────────────────────────
Pytest collection
  command: uv run --with pytest --with pyyaml --with 'mlflow[databricks]>=3.5,<4' \
    --with langgraph --with 'pydantic>=2' --with 'psycopg[binary]' \
    --with 'databricks-sdk>=0.81.0' pytest agent/tests --collect-only -q
  Result: 79 tests collected (was 77; +2 from tests/test_agent.py).

Pytest execution
  command: uv run --with pytest --with pyyaml --with 'mlflow[databricks]>=3.5,<4' \
    --with langgraph --with 'pydantic>=2' --with 'psycopg[binary]' \
    --with 'databricks-sdk>=0.81.0' pytest agent/tests -q
  Result: PASS — 77 passed, 2 skipped.

PASS: uv run --with ruff ruff check agent
  Result: All checks passed!
PASS: uv run --with ruff ruff format --check agent
  Result: 33 files already formatted.

── Eval gates (eval/ project env) ────────────────────────────────────────────────
Pytest collection
  command: (cd eval) uv run pytest --collect-only -q
  Result: 19 tests collected (was 9; +10 from tests/test_promote.py).

Pytest execution
  command: (cd eval) uv run pytest -q
  Result: PASS — 19 passed.

PASS: uv run --with ruff ruff check eval
  Result: All checks passed!
PASS: uv run --with ruff ruff format --check eval
  Result: 12 files already formatted.

── Notes ─────────────────────────────────────────────────────────────────────────
- MLflow version grounded against: 3.16.1 (installed in eval/.venv). API shapes for
  span.set_inputs/set_outputs, mlflow.update_current_trace(request_preview/response_preview),
  mlflow.get_trace / get_last_active_trace_id, mlflow.search_runs,
  MlflowClient.get_model_version_by_alias / set_registered_model_alias, and
  mlflow.set_active_model were verified by introspection, not guessed.
- No live evaluation was run and the live @prod alias was NOT moved. promote.py tests
  mock search_runs and the registry client, so no workspace call and no alias mutation.
