CREATE SCHEMA IF NOT EXISTS `fe-bar-ir`.bronze
Query executed successfully (no results)

CREATE SCHEMA IF NOT EXISTS `fe-bar-ir`.silver
Query executed successfully (no results)

CREATE SCHEMA IF NOT EXISTS `fe-bar-ir`.gold
Query executed successfully (no results)

CREATE OR REPLACE FUNCTION `fe-bar-ir`.silver.mask_customer(value STRING) RETURNS STRING RETURN CASE WHEN is_account_group_member('adjuster') OR is_member('admins') THEN value ELSE concat('REDACTED-', sha2(value, 256)) END
Query executed successfully (no results)

CREATE OR REPLACE FUNCTION `fe-bar-ir`.silver.mask_money(value DECIMAL(18,2)) RETURNS DECIMAL(18,2) RETURN CASE WHEN is_account_group_member('adjuster') OR is_member('admins') THEN value ELSE CAST(NULL AS DECIMAL(18,2)) END
Query executed successfully (no results)

GRANT USE CATALOG ON CATALOG `fe-bar-ir` TO `adjuster`
Query executed successfully (no results)

GRANT USE SCHEMA ON SCHEMA `fe-bar-ir`.silver TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.spec_standards TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.coating_warranty_terms TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.customers TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.suppliers TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.defect_codes TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.heats_coils TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.mill_test_certs TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.claims_history TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.adjudications_history TO `adjuster`
Query executed successfully (no results)

GRANT USE SCHEMA ON SCHEMA `fe-bar-ir`.gold TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.gold.claims_history TO `adjuster`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.gold.adjudications_history TO `adjuster`
Query executed successfully (no results)

GRANT USE CATALOG ON CATALOG `fe-bar-ir` TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT USE SCHEMA ON SCHEMA `fe-bar-ir`.silver TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.spec_standards TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.coating_warranty_terms TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.customers TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.suppliers TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.defect_codes TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.heats_coils TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.mill_test_certs TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.claims_history TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.silver.adjudications_history TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT USE SCHEMA ON SCHEMA `fe-bar-ir`.gold TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.gold.claims_history TO `metallurgy_analyst`
Query executed successfully (no results)

GRANT SELECT ON TABLE `fe-bar-ir`.gold.adjudications_history TO `metallurgy_analyst`
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.customers ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.customers ALTER COLUMN customer_name SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.customers ALTER COLUMN email SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.heats_coils ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.heats_coils ALTER COLUMN unit_price SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.claims_history ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.claims_history ALTER COLUMN unit_price SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.claims_history ALTER COLUMN claimed_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.claims_history ALTER COLUMN claimed_freight SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.claims_history ALTER COLUMN freight_cap SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.adjudications_history ALTER COLUMN claimed_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.bronze.adjudications_history ALTER COLUMN approved_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER TABLE `fe-bar-ir`.silver.customers ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER TABLE `fe-bar-ir`.silver.customers ALTER COLUMN customer_name SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER TABLE `fe-bar-ir`.silver.customers ALTER COLUMN email SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER TABLE `fe-bar-ir`.silver.heats_coils ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER TABLE `fe-bar-ir`.silver.heats_coils ALTER COLUMN unit_price SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.claims_history ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.claims_history ALTER COLUMN unit_price SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.claims_history ALTER COLUMN claimed_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.claims_history ALTER COLUMN claimed_freight SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.claims_history ALTER COLUMN freight_cap SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.adjudications_history ALTER COLUMN claimed_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.silver.adjudications_history ALTER COLUMN approved_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.claims_history ALTER COLUMN customer_id SET MASK `fe-bar-ir`.silver.mask_customer
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.claims_history ALTER COLUMN unit_price SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.claims_history ALTER COLUMN claimed_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.claims_history ALTER COLUMN claimed_freight SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.claims_history ALTER COLUMN freight_cap SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.adjudications_history ALTER COLUMN claimed_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)

ALTER MATERIALIZED VIEW `fe-bar-ir`.gold.adjudications_history ALTER COLUMN approved_amount SET MASK `fe-bar-ir`.silver.mask_money
Query executed successfully (no results)
